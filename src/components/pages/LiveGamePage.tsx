import LiveGamePageContent from "../game/LiveGamePageContent";
import MainContainer from "../main/MainContainer";

const LiveGamePage: React.FC = () => {
    return (
        <MainContainer>
            <LiveGamePageContent />
        </MainContainer>
    );
};

export default LiveGamePage;
